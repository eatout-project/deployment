apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restaurant-db-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: restaurant-db-sc
  resources:
    requests:
      storage: 5Gi